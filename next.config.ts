import type { NextConfig } from "next";

// Trigger environment validation during Next.js initialization
import "./src/env";

const nextConfig: NextConfig = {/* config options here */};

export default nextConfig;
